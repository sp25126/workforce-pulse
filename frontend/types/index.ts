export type AggregatesResponse = {
    // Placeholder type for Phase 2 ETL data
    status: string;
    data: any[]; 
};

export type FilterState = {
    // Placeholder type for filtering activity data
    dateRange: [string, string] | null;
    appNames: string[];
};
